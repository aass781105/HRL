# load_ppo.yml
# Configuration to run train_unified.py loading a pre-trained PPO model.

# PPO model path to load.
ppo_model_path: "cotrain_network/curriculum_train_10x5+mix.pth"

# Suffix for saved models and plot directory.
path_name: "load_ppo"

# You can add other parameters here if you need to override defaults from params.py
# For example:
# ddqn_episodes: 600
# seed_train: 100
